const theme = require('./theme')

module.exports = {
  theme,
  // corePlugins: ['pluginToKeep'],
  // corePlugins: {
  //   pluginToDisable: false
  // },
  references: {
    colors: {
      blue: 'colors.blue.500'
    }
  }
}
